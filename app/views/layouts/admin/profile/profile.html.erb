<!DOCTYPE html>
<html>
   <head>
      <title>Maxiambiental</title>
      <meta name="viewport" content="width=device-width,initial-scale=1">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>

      <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
      <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <%= javascript_importmap_tags %>
   </head>

   <body>

      <div data-controller="sidebar">
         <%= render 'layouts/admin/partials/header' %>
         <%= render 'layouts/admin/partials/sidebar' %>
      </div>

      <main>

         <div class="px-0 py-24 sm:ml-64 md:px-4">
            <div class="p-4">

               <%= rc 'flash\message' %>

               <%= render 'layouts/admin/profile/profile_section' %>

            </div>
         </div>
      </main>
   </body>
</html>