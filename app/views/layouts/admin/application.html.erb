<!DOCTYPE html>
<html>
   <head>
      <title>Maxiambiental</title>
      <meta name="viewport" content="width=device-width,initial-scale=1">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>

      <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
      <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <%= javascript_importmap_tags %>

      <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.snow.css" %>
      <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.js" %>
   </head>

   <body>

      <div data-controller="sidebar">
         <%= render 'layouts/admin/partials/header' %>
         <%= render 'layouts/admin/partials/sidebar' %>
      </div>

      <main>

         <div class="px-4 py-24 sm:ml-64">
            <div class="p-4">

               <%= rc 'flash\message' %>

               <%= yield %>

            </div>
         </div>
      </main>
   </body>
</html>