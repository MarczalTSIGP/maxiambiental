<%= simple_form_for [:admin, @course], 
                    html: { class: "space-y-4 md:space-y-6" } do |f| %>

   <%= f.input :name %>

   <%= f.input :description %>

   <%= f.input :image, as: :image %>

   <%= f.button :submit %>
<% end %>