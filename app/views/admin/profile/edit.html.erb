<turbo-frame id="user_profile">

   <div class="lg:mx-8 grid grid-cols-1 gap-4 m-auto p-4 lg:grid-cols-2 lg:p-4">

      <div class="w-full pb-8 mt-8 rounded-lg space-y-4 md:space-y-2">

         <h2 class="text-dark mb-2 text-lg font-semibold">
            <%= t('profile.edit_profile') %>
         </h2>

         <p class="text-body-color dark:text-dark-6 text-sm font-medium">
            <%= t('profile.edit_profile_description') %>
         </p>

      </div>

      <%= render 'admin/profile/partials/form_admin_data' %>

   </div>

</turbo-frame>