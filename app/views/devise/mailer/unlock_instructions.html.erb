<%= image_tag 'welcome_reset_password_instructions.png', alt: "Welcome image", width: "300" %>

<h1>Olá <%= @resource.email %>,</h1>

<p><%= t('devise.mailer.unlock_instructions.attention') %></p>

<p><%= t('devise.mailer.unlock_instructions.instructions')%></p>

<%= link_to t('links.unlock_my_account'), unlock_url(@resource, unlock_token: @token), class: 'button' %>
