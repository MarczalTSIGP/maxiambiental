<% provide(:head_title, "Redefinir Senha") %>
<% provide(:title, "Redefinir Senha") %>

<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), wrapper: :tailwind) do |f| %>
  <%= f.input :email, placeholder: t('simple_form.labels.email'), error: true %>

  <%= f.button :submit, "Enviar", class: "bg-green-700 text-white mt-5 cursor-pointer" %>
<% end %>

<%= render "devise/shared/links" %>
